# See the matching rootfs drop-in for why. The initrd needs this too: it's
# where systemd-repart formats/encrypts root against the software TPM on
# first boot, and that work can plausibly run past the hardware watchdog's
# 15s window on its own.
[Manager]
RuntimeWatchdogSec=14
