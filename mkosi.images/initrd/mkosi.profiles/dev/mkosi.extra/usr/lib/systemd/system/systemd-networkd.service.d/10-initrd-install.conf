# The stock unit only offers WantedBy=multi-user.target, which the initrd
# never reaches (it boots through initrd.target) - retarget at sysinit.target
# so the preset that enables this unit actually takes effect here.
[Install]
WantedBy=sysinit.target
