[Service]
# `systemd-sysupdate update` bails on a half-applied version ("Vacuum it to try
# installing again") - clear stale/incomplete instances first so a retry works.
ExecStartPre=-/usr/lib/systemd/systemd-sysupdate vacuum
