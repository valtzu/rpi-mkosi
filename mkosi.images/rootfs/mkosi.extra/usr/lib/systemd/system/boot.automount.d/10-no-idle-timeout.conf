[Automount]
# gpt-auto's ~2min idle timeout unmounts the ESP mid-sysupdate on slow USB
# media: PID 1 blocks in umount() long enough for the watchdog to reset the
# board, and 70-kernel.transfer fails when /boot vanishes underneath it.
TimeoutIdleSec=0
