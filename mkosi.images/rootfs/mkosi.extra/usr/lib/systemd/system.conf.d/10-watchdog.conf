[Manager]
# config.txt has dtparam=watchdog=on but nothing was arming /dev/watchdog.
# 14s matches Raspberry Pi OS (BCM2711 tops out ~15s). Pairs with the UKI's
# TriesLeft= counter: a hung "latest" update resets, the counter decrements,
# and it rolls back after 3 tries.
RuntimeWatchdogSec=14
