# dev: pull from this build host (mkosi serve) instead of GitHub releases.
# The base MatchPattern already lists both the .xz and the uncompressed name,
# so only the source URL changes here.
[Source]
Path=http://@BUILD_HOST@:8081
