[Timer]
# dev: check for updates ~1min after boot, then every 2min. The stock unit's
# RandomizedDelaySec=4h would otherwise smear these out, so pin it to 0.
OnBootSec=1min
OnUnitActiveSec=2min
RandomizedDelaySec=0
