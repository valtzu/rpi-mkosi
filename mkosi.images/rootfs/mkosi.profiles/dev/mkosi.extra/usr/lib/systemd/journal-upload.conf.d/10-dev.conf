# dev: ship this device's journal to the build host in real time. Pair with
# `systemd-journal-remote` listening there (systemctl enable --now
# systemd-journal-remote.socket) to read it back with `journalctl --merge -D
# /var/log/journal/remote/`. @BUILD_HOST@ is substituted by mkosi.finalize.
[Upload]
URL=http://@BUILD_HOST@:19532
