# The profile CI builds with, selected by resetting Profiles= to `disk,release`
# instead of the default `disk,dev` (see mkosi.conf). It carries no settings of
# its own yet - its job today is just to keep `dev` out of release/CI builds -
# but it's a real profile so release-only tweaks have somewhere to go later.
