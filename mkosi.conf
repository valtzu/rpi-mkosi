[Config]
MinimumVersion=25
Profiles=disk

[Build]
CacheDirectory=mkosi.cache
BuildDirectory=mkosi.builddir
WithNetwork=yes
Incremental=yes
ToolsTree=default
ToolsTreeDistribution=debian
ToolsTreeRelease=forky
ToolsTreeSnapshot=20260903T143241Z
ToolsTreePackages=
    python3-pycryptodome
    qemu-efi-aarch64
    unzip
    xxd
Environment=
    UEFI_VERSION=v1.53
    FIRMWARE_REF=eef9c230a0e70e23a54be98f90d5237ab6e0b0fd
    EEPROM_REF=86759b04b22173e10186139ac3ae4debcd0d7252
    EEPROM_VERSION=2026-08-04
    # systemd-repart's mkfs.ext4 otherwise picks a random directory hash seed,
    # which perturbs the /usr partition (and its verity roothash, and every UKI
    # and disk image that embeds it). Pin it for byte-reproducible output.
    # attached-arg form: mkosi splits Environment= entries on whitespace
    SYSTEMD_REPART_MKFS_OPTIONS_EXT4=-Ehash_seed=8c58b3b9-7383-50e6-aed7-8d8341fdaf5f

[Output]
OutputDirectory=mkosi.output
ManifestFormat=changelog

[Distribution]
Architecture=arm64
Distribution=debian
Release=forky
Snapshot=20260903T143241Z

[Content]
BaseTrees=
    %O/rootfs_%v

[Runtime]
RuntimeSize=8G
RAM=4G
KernelCommandLineExtra=
	# slow cross-arch emulation requires increased timeouts
	systemd.default_device_timeout_sec=300
TPM=no # Raspberry Pi has no TPM either
Ephemeral=yes
Credentials=
    cryptsetup.passphrase=123
[Validation]
Checksum=no
SecureBoot=yes
SecureBootKey=%D/mkosi.key
SecureBootCertificate=%D/mkosi.crt
VerityKey=%D/mkosi.key
VerityCertificate=%D/mkosi.crt
SignExpectedPcr=no # EDK2 does not measure anything – maybe u-boot chain?
